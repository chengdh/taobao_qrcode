- qr = item_qrcode(item)
%table.qrcode
  - qr.modules.each_index do |x|
    %tr  
      - qr.modules.each_index do |y|
        - if qr.dark?(x,y)
          %td.black
        - else 
          %td.white
  %tr
    %td(colspan='100')
      = item.title

